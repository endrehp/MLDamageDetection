close all

% MAC comparison

ne = 100;
ns = 9;
damage = [30, 50];
f = [0.1, 0.2];

[uV, uD] = damagedBeam(ne, ns, damage, 0);
[dV, dD] = damagedBeam(ne, ns, damage, f);

L = length(uV(1,:));
macs = zeros(1, L);

for i=1:L
    
    